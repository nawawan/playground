steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${_PROJECT}/${_REPO_ID}/nawawan-playground:latest'
      - '-f'
      - './backend/Dockerfile'
      - '.'

images:
  - '${_REGION}-docker.pkg.dev/${_PROJECT}/${_REPO_ID}/nawawan-playground:latest'

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _REGION: asia-northeast1
  _PROJECT: nawawan
  _REPO_ID: nawawan-prod-repository
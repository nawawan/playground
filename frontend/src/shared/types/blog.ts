export type BlogResponse = {
    id: string;
    title: string;
    content_key: string;
    status: string;
};

export type BlogDetails = {
    id: string;
    title: string;
    content_html: string;
}